#!groovy
env.submitter = ''
env.projectid = ''

pipeline {
    agent any

    stages {
        stage("App Build") {
            steps {
                script {
                    println("************[App Build]************")

                }
            }
        }

        stage("Docker Build & Push") {
            steps {
                script {
                    println("************[Docker Build & Push]************")
                }
            }
        }

    }
}